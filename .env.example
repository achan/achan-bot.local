# achan-bot.local setup configuration
# Copy to .env and edit before running ./setup.sh

# User account to create (non-admin)
BOT_USER="claude"
BOT_USER_FULLNAME="Claude"
BOT_USER_SHELL="/bin/zsh"

# Git identity for the bot user
GIT_USER_NAME=""
GIT_USER_EMAIL=""

# Hostname (used in output/instructions only)
BOT_HOSTNAME="achan-bot.local"

# Workspace directory (created under /Users/$BOT_USER/)
WORKSPACE_DIR="src"

# mkcert certificate location (under /Users/$BOT_USER/)
CERT_DIR=".local/share/mkcert"
CERT_FILE="localhost.pem"
CERT_KEY_FILE="localhost-key.pem"

# mkcert SANs (domains/IPs the cert is valid for)
CERT_SANS="localhost 127.0.0.1 ::1"
