# Bot provisioning configuration
# Copy to .env and edit before running ./setup.sh

# Bot type: "remote" (SSH access from another machine) or "local" (Standard user on this machine)
BOT_TYPE="remote"

# GitHub username for the bot — used to fetch SSH public keys and git identity
GITHUB_USER=""

# GitHub username of the admin/owner — SSH keys are fetched so they can SSH in
# (remote only)
ADMIN_GITHUB_USER=""

# User account to create (non-admin)
BOT_USER="claude"
BOT_USER_FULLNAME="Claude"
BOT_USER_SHELL="/bin/zsh"

# Hostname — used in output/instructions for remote bots (remote only)
BOT_HOSTNAME="achan-bot.local"

# Workspace directory (created under /Users/$BOT_USER/)
WORKSPACE_DIR="repos"

# SSH public key for authorized_keys (remote only)
# Used as fallback if the GitHub user has no keys uploaded yet.
# e.g. BOT_SSH_PUBLIC_KEY="ssh-ed25519 AAAA... comment"
BOT_SSH_PUBLIC_KEY=""
